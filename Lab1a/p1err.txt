2 lexical errors in computing sol for 2nd order eq

- identifier can't start with a digit
- integer does not exist (correct form is int)

application{
	1sol:int; sol2:int; 
	a:integer; b:integer; c:integer;
	read(a); read(b); read(c);
	1sol := (b*b + 4*a*c)/2;
	sol2 := (b*b - 4*a*c)/2;
	write(1sol);
	write(sol2);
}